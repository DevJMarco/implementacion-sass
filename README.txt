Modificaciones SASS:

-Cambio de la carpeta scss por la carpeta sass y empleo de una estructura adecuada en la misma.
-Creacion de extend en el archivo _extend 
-Creacion de mixins en el archivo _mixin y uso del mismo en varios archivos de la carpeta layout/
-Creacion de un map en el archivo _varibles y uso del mismo en el archivo _paleta.scss
-Creacion de otro map en el archivo _varibles y uso del mismo en el archivo _productos.scss

Modificaciones SEO:

-
-
-
-